# Deployment

The canonical deployment target for the MVP is:

- FastAPI API + Telegram webhook: Render web service
- Worker scheduler: Render background worker
- Mini App: Cloudflare Pages or Render static site
- PostgreSQL: Render PostgreSQL or any managed Postgres

The detailed deployment checklist lives in `docs/DEPLOY.md`.
